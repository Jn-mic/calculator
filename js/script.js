var add = function(number1, number2) {
    return number1 + number2;
  };
  var number1=perseInt(prompt("What is your number:"));
  var number2=perseInt(prompt("Type in your another Number:"));
   var result= add(number1,number2);
   alert(result);
  